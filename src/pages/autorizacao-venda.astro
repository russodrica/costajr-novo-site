---
// Endereço antigo do formulário. O link que a Adriana envia agora é o curto
// (/cr), com a marca CR e sem menção à Costa Júnior — este redirecionamento
// existe só para não quebrar nada que já tenha sido compartilhado.
export const prerender = false;
return Astro.redirect("/cr", 302);
---
